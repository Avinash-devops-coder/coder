pipeline {
	agent any
		stages {
			stage ('Build') {
			steps {
		'This stage is used for building'
			}
		}
			stage ('Test') {
			steps {
		'This stage is used for testing'
			}
		}
			stage ('Execute') {
			steps {
		'This stage is used for executing'
			}
		}
}
}

